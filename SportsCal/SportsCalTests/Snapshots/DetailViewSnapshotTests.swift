//
//  DetailViewSnapshotTests.swift
//  SportsCalTests
//
//  Hero detail view coverage lives in ScreenSnapshotTests.swift now.
//  Keep this file as a stub so the test plan still discovers the target.
//

#if canImport(SnapshotTesting) && os(iOS)
import XCTest

@MainActor
final class DetailViewSnapshotTests: XCTestCase {
    func test_placeholder() {
        XCTAssertTrue(true)
    }
}
#endif
