# SportsCal scripts environment — copy to `.env` and fill in.
# `.env` is gitignored; this template is committed.

# Base URL for the SportsCal API server (no trailing slash).
# Production: https://api.sportscal.app
# Tailscale dev: http://100.68.255.93:8080
SPORTSCAL_API_BASE=https://api.sportscal.app

# Client API key — same value used by SportsCal/Shared/Constants.swift.
SPORTSCAL_API_KEY=
